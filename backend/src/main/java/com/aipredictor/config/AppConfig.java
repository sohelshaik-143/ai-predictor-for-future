package com.aipredictor.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Other beans can go here
    // PasswordEncoder is now only in SecurityConfig
}